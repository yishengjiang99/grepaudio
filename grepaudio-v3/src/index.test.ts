import { expect } from "chai";

describe("2+2", () => {
	it("simple math", () => {
		expect(2 + 2).to.equal(4);
	});
});
describe("minus 1 is 3", () => {
	it("simple math", () => {
		expect(4 - 1).to.equal(3);
	});
});
