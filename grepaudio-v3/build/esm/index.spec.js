import { expect } from "chai";
describe("2+2", () => {
    it("simple math", () => {
        expect(2 + 2).to.equal(4);
    });
});
describe("minus 1", () => {
    it("simple math", () => {
        const ctx = expect(2 + 2).to.equal(4);
    });
});
//# sourceMappingURL=index.spec.js.map