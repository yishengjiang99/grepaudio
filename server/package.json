{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "lsof -i tcp:8086 |awk '{print $2}' |grep -v PID|xargs kill -9; lsof -i tcp:3333 |awk '{print $2}' |grep -v PID|xargs kill -9",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.0.20",
    "express": "^4.17.1",
    "ffmpeg": "0.0.4",
    "fluent-ffmpeg": "^2.1.2",
    "request-promise": "^4.2.5",
    "ws": "^7.2.3",
    "ytdl": "^1.3.0"
  }
}
